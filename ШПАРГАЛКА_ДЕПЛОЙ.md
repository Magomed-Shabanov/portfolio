# 📋 Шпаргалка: Git и Деплой

## ⚡ Самый быстрый способ (после изменений):

```powershell
.\deploy.ps1 "Описание ваших изменений"
```

**Или вручную:**
```powershell
git add .
git commit -m "Описание изменений"
git push
# Vercel задеплоит автоматически!
```

---

## 🔧 Первая настройка (один раз):

### Если Git ещё не настроен:

```powershell
# 1. Инициализация
git init
git add .
git commit -m "Initial commit"

# 2. Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main

# 3. В Vercel Dashboard:
# Settings → Git → Connect Repository
```

---

## 📝 Примеры использования:

```powershell
# Обновили Footer
.\deploy.ps1 "Обновлён дизайн Footer"

# Добавили новую секцию
.\deploy.ps1 "Добавлена секция Projects"

# Исправили ошибку
.\deploy.ps1 "Исправлена ошибка в Hero компоненте"
```

---

## 🚀 Ручной деплой (если Git не подключён):

```powershell
vercel --prod
```

---

## 📚 Подробные инструкции:

- **Полная инструкция:** `ИНСТРУКЦИЯ_GIT_И_ДЕПЛОЙ.md`
- **Быстрый деплой:** `БЫСТРЫЙ_ДЕПЛОЙ.md`

---

## ✅ Чеклист:

- [ ] Изменения сохранены
- [ ] `git add .` выполнен
- [ ] `git commit -m "..."` выполнен
- [ ] `git push` выполнен
- [ ] Проверен статус в Vercel Dashboard

---

**Готово! Ваши изменения задеплоены! 🎉**


# 📦 Инструкция: Git и Деплой изменений

## 🎯 Быстрый процесс (после внесения изменений)

```powershell
# 1. Проверить изменения
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит
git commit -m "Описание ваших изменений"

# 4. Отправить в GitHub
git push

# 5. Vercel автоматически задеплоит (если подключен Git)
# ИЛИ вручную:
vercel --prod
```

---

## 📋 Подробная инструкция

### Вариант 1: Если Git уже настроен (репозиторий существует)

#### Шаг 1: Проверка статуса
```powershell
git status
```
Покажет все изменённые, добавленные и удалённые файлы.

#### Шаг 2: Добавление изменений
```powershell
# Добавить все изменения
git add .

# ИЛИ добавить конкретные файлы
git add components/Footer.tsx
git add app/page.tsx
```

#### Шаг 3: Создание коммита
```powershell
git commit -m "Обновлён футер и главная страница"
```

**Хорошие сообщения коммитов:**
- ✅ "Добавлена анимация в Hero секцию"
- ✅ "Исправлена ошибка в компоненте Footer"
- ✅ "Обновлён дизайн страницы About"
- ❌ "Изменения" (слишком общее)
- ❌ "fix" (неинформативно)

#### Шаг 4: Отправка в GitHub
```powershell
# Если это первый push или нужно указать ветку
git push origin main

# ИЛИ если уже настроена ветка по умолчанию
git push
```

#### Шаг 5: Деплой в Vercel

**Если Git подключён к Vercel (автоматический деплой):**
- ✅ Vercel автоматически начнёт деплой после `git push`
- Проверьте статус: https://vercel.com/dashboard
- Обычно деплой занимает 1-3 минуты

**Если Git НЕ подключён (ручной деплой):**
```powershell
vercel --prod
```

---

### Вариант 2: Если Git ещё не настроен (первая настройка)

#### Шаг 1: Инициализация Git
```powershell
git init
```

#### Шаг 2: Добавление файлов
```powershell
git add .
```

#### Шаг 3: Первый коммит
```powershell
git commit -m "Initial commit: портфолио сайт"
```

#### Шаг 4: Создание репозитория на GitHub

1. Зайдите на https://github.com/new
2. Введите название репозитория (например: `portfolio`)
3. **НЕ** ставьте галочки на "Add README", "Add .gitignore" (у вас уже есть файлы)
4. Нажмите "Create repository"

#### Шаг 5: Подключение к GitHub
```powershell
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# ИЛИ если используете SSH
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git
```

**Пример:**
```powershell
git remote add origin https://github.com/Magomed-Shabanov/portfolio.git
```

#### Шаг 6: Отправка кода
```powershell
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен доступа).

#### Шаг 7: Подключение Vercel к GitHub (для автоматических деплойментов)

1. Зайдите на https://vercel.com/dashboard
2. Найдите ваш проект "portfolio"
3. Перейдите в **Settings** → **Git**
4. Нажмите **Connect Git Repository**
5. Выберите ваш GitHub репозиторий
6. Подтвердите подключение

**Теперь каждый `git push` будет автоматически деплоить проект! 🚀**

---

## 🔄 Типичный рабочий процесс

### Ежедневная работа:

```powershell
# 1. Внесли изменения в код
# (редактировали файлы в редакторе)

# 2. Проверили что изменилось
git status

# 3. Добавили изменения
git add .

# 4. Закоммитили
git commit -m "Добавлена новая секция Projects"

# 5. Отправили в GitHub
git push

# 6. Vercel автоматически задеплоит (если подключён Git)
# Или вручную:
vercel --prod
```

---

## 🛠️ Полезные команды Git

### Просмотр изменений
```powershell
# Статус файлов
git status

# Что изменилось в файлах
git diff

# История коммитов
git log

# Краткая история
git log --oneline
```

### Отмена изменений
```powershell
# Отменить изменения в файле (до git add)
git checkout -- filename

# Убрать файл из staging (после git add, до commit)
git reset HEAD filename

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1
```

### Работа с ветками
```powershell
# Создать новую ветку
git branch feature/new-section

# Переключиться на ветку
git checkout feature/new-section

# Создать и переключиться
git checkout -b feature/new-section

# Посмотреть все ветки
git branch

# Слить ветку в main
git checkout main
git merge feature/new-section
```

---

## 🚀 Деплой в Vercel

### Автоматический деплой (рекомендуется)

**Настройка один раз:**
1. Vercel Dashboard → Settings → Git
2. Подключите GitHub репозиторий

**После этого:**
- Каждый `git push` → автоматический деплой
- Каждая ветка → preview деплоймент
- Каждый PR → preview деплоймент

### Ручной деплой

```powershell
# Production деплой
vercel --prod

# Preview деплой (для тестирования)
vercel

# Деплой конкретной ветки
vercel --prod --branch=main
```

### Проверка статуса деплоймента

```powershell
# Посмотреть информацию о последнем деплое
vercel inspect

# Или в браузере:
# https://vercel.com/dashboard → ваш проект → Deployments
```

---

## ⚠️ Частые проблемы и решения

### Проблема: "fatal: not a git repository"
**Решение:** Выполните `git init` в папке проекта

### Проблема: "fatal: remote origin already exists"
**Решение:** 
```powershell
git remote remove origin
git remote add origin https://github.com/USERNAME/REPO.git
```

### Проблема: "Permission denied"
**Решение:** 
- Проверьте логин/пароль GitHub
- Или используйте Personal Access Token вместо пароля

### Проблема: "nothing to commit"
**Решение:** 
- Убедитесь, что файлы сохранены
- Проверьте `git status` - возможно изменения уже закоммичены

### Проблема: Деплой не запускается автоматически
**Решение:**
1. Проверьте подключение Git в Vercel Dashboard
2. Убедитесь, что делаете `git push` в правильную ветку (обычно `main`)
3. Проверьте логи в Vercel Dashboard

---

## 📝 Примеры хороших коммитов

```powershell
# Добавление новой функции
git commit -m "Добавлена секция Projects с анимацией"

# Исправление ошибки
git commit -m "Исправлена ошибка отображения в Footer на мобильных"

# Обновление стилей
git commit -m "Обновлён дизайн Hero секции"

# Рефакторинг
git commit -m "Рефакторинг компонента Navbar для лучшей производительности"

# Обновление зависимостей
git commit -m "Обновлены зависимости: Next.js 15.1.0, React 19"
```

---

## 🎯 Чеклист перед деплоем

- [ ] Изменения протестированы локально (`npm run dev`)
- [ ] Сборка работает (`npm run build`)
- [ ] Все изменения добавлены (`git add .`)
- [ ] Создан коммит с понятным сообщением
- [ ] Код отправлен в GitHub (`git push`)
- [ ] Проверен статус деплоймента в Vercel Dashboard
- [ ] Сайт работает на Production URL

---

## 🔗 Полезные ссылки

- [Git документация](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Vercel Git Integration](https://vercel.com/docs/concepts/git)
- [Vercel Dashboard](https://vercel.com/dashboard)

---

**Теперь вы знаете весь процесс! 🎉**

После каждого изменения:
1. `git add .`
2. `git commit -m "описание"`
3. `git push`
4. Vercel задеплоит автоматически!


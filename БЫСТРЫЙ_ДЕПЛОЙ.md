# ⚡ Быстрый деплой изменений

## После внесения изменений в проект:

```powershell
# 1. Добавить все изменения
git add .

# 2. Создать коммит
git commit -m "Описание изменений"

# 3. Отправить в GitHub
git push

# 4. Vercel задеплоит автоматически! 🚀
```

**Или если Git не подключён:**
```powershell
vercel --prod
```

---

## Первая настройка (один раз):

```powershell
# 1. Инициализировать Git
git init
git add .
git commit -m "Initial commit"

# 2. Создать репозиторий на GitHub и подключить:
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main

# 3. В Vercel Dashboard подключить Git репозиторий
# Settings → Git → Connect Repository
```

**Подробная инструкция:** `ИНСТРУКЦИЯ_GIT_И_ДЕПЛОЙ.md`

